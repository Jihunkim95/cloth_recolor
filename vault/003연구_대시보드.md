# 연구 대시보드

## 가제

> **ClothSplat: Text-Driven Garment Recoloring in 4D Gaussian Splatting**
>
> (확정 2026-05-13 — 7 후보 중 application-first 방향 선택. *short method name + 명확한 task + zero-shot SAM3 함축*.)

## 한 줄 요약

> **4D GS 가우시안 단위로 옷 라벨 ([[notes/cloth_logit_channel]]) 을 학습시켜서, 학습 후 SH_DC HSV-swap 만으로 옷 색을 시간·시점 일관되게 바꾸는 연구.**
>
> Input: [[notes/4d_gaussian_splatting]] 논문이 쓰는 D-NeRF 데이터셋 (multi-frame 이미지 + pose + time)
> Process: [[notes/sam3_mask_supervision]] 로 옷 mask supervise + [[notes/soft_label_calibration]] 로 경계 모호성 처리
> Output: 학습된 4DGS ckpt + [[notes/hsv_recolor_trick]] 로 옷만 recolor 한 PLY/PNG/video

## 실험 결과 (results/)
```dataview
TABLE status, date, code
FROM "results"
WHERE status != null
SORT date DESC, file.name DESC
```

## 깰 수 있는 것들 (Layer 2)
```dataview
TABLE layer, derived_from
FROM ""
WHERE contains(string(layer), "2")
SORT file.name ASC
```

## 깰 수 없는 것들 (Layer 0)
```dataview
TABLE layer, derived_from
FROM ""
WHERE contains(string(layer), "0")
SORT file.name ASC
```

## 전체 노트
```dataview
TABLE layer, date
FROM ""
WHERE layer != null
SORT date DESC
```